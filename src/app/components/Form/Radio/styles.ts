import style9 from "style9";

export const { ...classes } = style9.create({
  label: {
    display: "inline-flex",
    alignItems: "center",
    cursor: "pointer",
  },
  labelName: {
    marginLeft: "0.5rem",
  },
  radio: {
    cursor: "pointer",
  },
});
