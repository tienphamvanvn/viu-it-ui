import style9 from "style9";

export const { ...classes } = style9.create({
  shortInfoContainer: {
    flexDirection: "row",
    justifyContent: "space-between",
    width: "100%",
  },
  shortInfoCenter: {
    textAlign: "center",
  },
});
